#!/usr/bin/ruby
# rnm
# by nick

require_relative '../lib/rnm'

if ARGV.length < 3 then
  puts "usage: rnm threads address port"
  exit
end

RNM::Application.new(ARGV[0].to_i, ARGV[1], ARGV[2].to_i).run
